# 동글사각 | CIRCLE SQUARE — 운영 정책

세라믹 도자기 컨셉 디자인 갤러리. 공개 주소: https://suokkim.github.io/ceramic/

## 1. 폴더 구조
| 폴더 | 용도 | 공개 여부 |
|---|---|---|
| `kra/` | Krita 원본 작업 파일 | 로컬 전용 (git 동기화 안 함) |
| `png/` | 원본 PNG 보관 (Krita가 내보낸 이름 그대로) | 로컬 전용 (git 동기화 안 함) |
| `docs/` | GitHub Pages 사이트 루트 (웹 최적화 이미지 포함) | 공개 |

## 2. 작업 시작: 새 캔버스 만들기
```
./newdoc.sh
```
- `kra/MMDD_NN.kra`를 만들고 Krita로 연다 (Krita가 꺼져 있으면 켜진다).
- 규격: **A4 300dpi, 2480×3508, RGB 8bit** — 저장소의 `template.kra`를 복사한다.
  구성: 흰 배경 + 빈 그림 레이어 + **숨긴 `sil` 레이어**(수제 실루엣용, 4절 참조).
  실루엣을 손으로 그릴 작품이면 sil 레이어에 그리고 `krita-export-sil.py` 단축키로
  내보내면 된다. 안 쓰면 그냥 두면 된다(숨겨져 있어 내보내기에 안 섞인다).
  template.kra는 Krita 기본 A4 템플릿에 sil 레이어를 심은 것(zip 직접 편집).
- **이름 규칙: `MMDD_NN.kra` — 앞은 만든 날짜, 뒤 NN은 갤러리에서 받게 될 번호.**
  `png/`의 마지막 번호(와 아직 안 내보낸 kra 번호) 다음 값을 미리 붙인다 →
  `0806_08.kra`로 그린 그림은 `0806_08.png`로 내보내지고 갤러리 08번이 된다.
  번호를 되돌리면 내보낸 PNG가 이전 작품을 덮어쓴다.
  **Krita에 열려 있는 파일은 이름을 바꾸지 말 것** — Krita가 옛 경로로 되살려낸다.
  ※ 06번은 수정 이력 때문에 `0803_06.kra`·`0804_06.kra` 두 개가 같은 번호를 쓰고
  날짜로 구분된다.
- **다음 번호는 `png/`와 `kra/`를 **둘 다** 보고 정한다.** 하루에 여러 장을 내보내면
  kra 하나에 png가 여러 개라(예: `0807_09.kra` 하나에 09~12번 png) kra만 보면 이미 쓴
  번호를 다시 발급한다. 실제로 `newdoc.sh`가 `NN.png`만 매칭하는 정규식을 따로 들고 있어
  `MMDD_NN.png`인 `png/`를 통째로 못 읽었고, 12번까지 있는데 `0808_10.kra`를 만들었다.
- **번호 읽는 규칙은 `numof.sh` 한 곳에만 둔다.** `newdoc.sh`·`optimize.sh`가 이걸
  `. ./numof.sh`로 불러 쓴다 — 각자 정규식을 들고 있으면 어긋나서 위 사고가 재발한다.
  고친 뒤에는 `sh numof.sh --test`로 검사할 것.
- 규격을 바꾸려면 `template.kra`를 교체하면 된다(`newdoc.sh`의 `TPL`).
- 같은 날 하나 더 만들려면 `./newdoc.sh -n`. 인자 없이 실행하면 오늘 것이 이미 있을 때
  새로 만들지 않고 열기만 한다(자동 실행이 여러 번 돌아도 안전하게).

### 매일 아침 8시 자동 실행 (설정 완료)
- `~/Library/LaunchAgents/com.ceramic.newdoc.plist` — 매일 08:00에 실행. 로그 `/tmp/ceramic-newdoc.log`.
- **스크립트를 직접 돌리지 않고 `~/Applications/Ceramic 새 작업.app`을 `open`으로 띄운다.**
  launchd가 `/bin/sh`로 직접 실행하면 macOS가 `~/Documents` 접근을 막아
  (`Operation not permitted`) 아무것도 못 한다. 앱을 거치면 권한 주체가 앱이 되어 통과한다.
- 끄기: `launchctl bootout gui/$UID/com.ceramic.newdoc`
  다시 켜기: `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.ceramic.newdoc.plist`

## 3. 이미지 업로드 정책
- **포맷: PNG만.** JPG 등 다른 포맷 금지.
- **파일명: `MMDD_NN.png`** — kra와 똑같다 (`0806_08.kra` → `0806_08.png`).
  Krita 내보내기 기본값이 문서 이름이므로 **그냥 내보내면 규칙에 맞는다.**
- **`png/`의 파일 이름은 절대 바꾸지 않는다.** 번호로 바꿔버리면 다음 내보내기가
  또 Krita 이름으로 떨어져 같은 작품이 새 번호를 받는다 — 실제로 한 작품이 두 번
  올라간 적이 있다. `optimize.sh`는 이름에서 **번호만 읽어** `docs/images/NN.png`로
  복사한다. 공개되는 웹용만 번호로 통일하는 이유는 갤러리 순서가 파일명 정렬이라
  날짜가 끼면 꼬이기 때문이다.
- 번호를 못 읽는 파일(Krita 밖에서 넣은 것)에만 `MMDD_NN`으로 이름을 붙여준다.
- 옛 이름(`0806_s08.png`)도 번호로 읽는다 — Krita에 열려 있던 문서가 옛 경로로
  저장되는 경우가 있어서다.
- **갤러리 표시 순서: 번호 역순** (큰 번호 = 최신 작업이 맨 앞).
- 순서 변경: `png/`에서 번호를 직접 수정 후 `./optimize.sh` 재실행.
- 원본은 절대 리사이즈하지 않는다. 웹용 사본만 `docs/images/`에 생성.

### 기존 이미지 수정(재수출) 시
- **그냥 다시 내보내면 된다.** Krita 문서 이름이 그대로이므로 `png/`의 같은 파일을
  덮어쓰고, 번호도 그대로라 갤러리의 그 자리만 갱신된다.
- 같은 번호를 가진 파일이 어쩌다 둘이 되면 **최신 파일이 이긴다**.
- 이미지 주소에 내용 해시(`?v=`)가 붙으므로 수정본은 브라우저 캐시 없이 즉시 반영된다.

## 4. 웹 최적화 규격
- 네 종류를 만든다: **작품 `docs/images/NN.png`**(확대 보기용),
  **썸네일 `docs/images/th/NN.jpg`**(대문 그리드용),
  **정사각 `docs/images/sq/NN.jpg`**(폰 모아보기 전용),
  **실루엣 `docs/images/si/NN.svg`**(모아보기 겹층용).
- **작품: 긴 변 1600px, PNG** (macOS 내장 `sips`) — 확대 보기에 충분.
- **썸네일: 긴 변 600px, JPEG q82.** 축소와 변환을 `sips -Z 600 -s format jpeg`
  한 번에 처리할 것 — 나눠 하면 JPEG를 두 번 인코딩하게 된다.
  - PNG였을 때 15장 1.06MB였다. 모아보기가 기본이 되면서 그리드 전체가 한 화면에
    들어와 `loading="lazy"`가 아무것도 못 미루고 전부 첫 로딩에 받는다. JPEG q82로
    0.40MB(2.6배). 화질 손실은 배경 오차 0, 오브제 ±1로 눈에 안 보인다.
  - **작품 파일과 `png/` 원본은 PNG 그대로.** JPEG는 썸네일에만 쓴다.
  - `images.json`은 작품 이름(`NN.png?v=…`)만 들고 있고, 그리드는 확장자만 바꿔
    참조한다(`name.replace('.png','.jpg')`).
- **정사각: 240×240, JPEG q82.** 모아보기에서 칸이 충분히 작을 때 쓴다 (16장 0.25MB).
  - **어느 변형을 받을지는 기기가 아니라 `칸 실제 폭 × DPR ≤ 240`으로 정한다.**
    폰/데스크톱으로 나누면 데스크톱 10열(칸 108px = 레티나 216px)처럼 작은 경우를
    놓쳐 424px짜리를 헛되이 받는다. 경계는 폰이면 3열부터, 데스크톱이면 10열부터 sq.
  - 그리드 폭은 **재서** 얻는다(`getBoundingClientRect`). CSS의 `max-width`·padding을
    JS에 복제하면 한쪽만 고쳤을 때 조용히 어긋난다. gap(2px)만 JS에 적혀 있다.
  - 밀도만 바꿔도 경계를 넘으면 다시 그려 변형을 교체한다.
  - **크롭과 축소를 한 번에 부르지 말 것.** `sips -c W W -Z 240`으로 주면 sips가
    `-Z`를 먼저 적용해 169×169 같은 엉뚱한 크기가 나온다. 두 번에 나눠 호출한다.
  - 큰 보기는 세로 비율이 필요해서 정사각을 못 쓴다 — 항상 th.
- **실루엣: SVG 벡터** — 흰 배경 rect + 회색(#666) 오브제 패스, `fill-rule=evenodd`
  (슬릿·구멍 자동 처리). 마스크 경계를 폴리곤으로 추출해 RDP(ε=w/200)로 단순화 —
  래스터와 달리 칸이 아무리 작아도 계단·지글거림이 없다. 장당 300~600바이트.
  `silhouette.mjs`(node, 의존성 없음 — 이미지 읽기는 sips로 BMP 변환)가
  **웹용 1600px PNG에서** 생성한다. `optimize.sh`가 원본이 더 새것일 때만(-nt) 다시 만든다.
  - **왜 오프라인인가**: 처음엔 브라우저가 240px JPEG 썸네일에서 매번 계산했는데,
    JPEG 노이즈와 저해상도 때문에 의도된 슬릿(5px)과 질감 균열(5px)이 같은 크기로
    보여 휴리스틱이 계속 늘었다. 1600px 무압축에서 한 번만 계산하면 같은 규칙이
    안정적으로 돌고, **결과가 파일이라 눈으로 검수·문제 작품만 개별 수정 가능**하다.
  - 파이프라인(각 단계 한 가지 결함 담당): 어두움 판정(값≤215 또는 블러 평균≤205)
    → 클로징+테두리 flood fill → 오프닝 → 잔점 제거 → 갇힌 웅덩이 메꾸기(단
    폭≥높이×3 무리는 의도된 슬릿으로 보고 보존) → 바닥 그림자 제거(수렴까지 반복).
    파라미터는 전부 이미지 크기에 비례 — 상세는 `silhouette.mjs` 주석.
  - **수제 실루엣 오버라이드**: 자동 결과가 마음에 안 들면 Krita에서 실루엣을
    이름이 **`sil`**인 레이어에 직접 그리고 `krita-export-sil.py` 단축키로 내보낸다
    (아래 참조). 스크립트가 `png/MMDD_NN_sil.png`(sil 레이어)와 `png/MMDD_NN.png`
    (sil 제외 작품)를 한 번에 저장한다. 스크립트 없이 손으로 하려면:
    레이어 선택 → 레이어 > 가져오기/내보내기 > 레이어 저장 → `png/MMDD_NN_sil.png`.
  - **`krita-export-sil.py` 설치(한 번)**: 설정 > Krita 구성 > Python 플러그인
    관리자 > "Ten Scripts" 켜고 재시작 → 도구 > 스크립트 > Ten Scripts 설정에서
    이 파일을 슬롯에 지정(단축키 Ctrl+Shift+숫자). sil 레이어가 없는 문서에서
    눌러도 작품만 내보내므로 모든 작업에 같은 키를 써도 된다.
  - **하루 한 파일에 작품 여러 개**를 그릴 때는 **최상위 레이어(그룹) 이름을 번호로**
    짓는다: `0809_18`, `0809_19`, 실루엣은 `0809_18_sil`. 단축키 한 번이면 번호
    레이어마다 한 장씩(그 레이어 + 배경 등 공용 레이어만 켜고) 내보내고, `_sil`
    레이어는 각각 실루엣으로 짝지어 저장한다. 번호 없는 최상위 레이어는 공용(배경).
    번호 레이어가 하나도 없으면 문서 파일명이 번호인 단일 모드로 동작한다(템플릿의
    `sil` 레이어는 단일 모드 전용 — 다작품 모드에선 `번호_sil`로 이름 지을 것).
    빈 실루엣 레이어는 없는 것으로 치고 자동 계산에 맡긴다.
    있으면 자동 계산 대신 그린 그대로 벡터화한다(`--manual`: 휴리스틱 전부 생략,
    어두운 칠 = 오브제, 안 칠한 곳·투명 = 배경, 구멍도 그린 대로).
    자동 업로드 감시가 png/를 보므로 저장만 하면 반영까지 자동이다.
    수제를 그만두려면 `_sil` 파일과 `docs/images/si/NN.svg`를 지우고 `./optimize.sh`.
    `_sil` 파일은 작품으로 취급되지 않는다(자동 넘버링·갤러리 목록에서 제외).
  - 특이한 신작(배경만큼 밝은 오브제, 가늘고 긴 세로 틈 등)에서 실루엣이 어긋나면
    전체 규칙을 비틀지 말고 위 수제 오버라이드로 그 작품만 해결한다.
- **원본이 바뀐 것만** 다시 만든다. `sips` 출력은 같은 입력이라도 실행할 때마다 바이트가
  달라져서(내부 타임스탬프) 전부 재생성하면 바뀐 게 없어도 커밋이 2MB씩 쌓인다.
  판단 기준은 원본 해시 기록 `docs/.imghash`. 원본에서 사라진 웹 이미지는 지운다.
- `docs/images.json`이 갤러리 목록(역순 정렬)의 단일 소스. `?v=`는 **원본** 해시라
  실행할 때마다 흔들리지 않고 그림을 실제로 고쳤을 때만 바뀐다.
- 링크 공유 미리보기(`og:image`)도 `optimize.sh`가 **최신 작품으로 자동 갱신**한다.
  주소가 바뀌면 `optimize.sh`의 sed 한 줄과 `index.html`의 `og:url`을 같이 고칠 것.

## 5. 업로드 절차 — 자동
Krita에서 `png/`로 내보내기만 하면 끝이다. 나머지는 감시 에이전트가 처리한다.

- `~/Library/LaunchAgents/com.ceramic.autopush.plist` — `png/` 폴더가 바뀌면
  `autopush.sh` 실행(앱 `~/Applications/Ceramic 자동업로드.app` 경유, 2절과 같은 이유).
  로그 `/tmp/ceramic-autopush.log`.
- `autopush.sh`: 파일 크기가 멈출 때까지 대기(내보내는 중인 파일 방지) →
  `optimize.sh` → 바뀐 게 있을 때만 커밋·푸시. 1~2분 뒤 사이트 반영.
- 손으로 하려면: `./optimize.sh` 후 커밋·푸시. 결과는 같다.
- 끄기: `launchctl bootout gui/$UID/com.ceramic.autopush`

## 6. 디자인 컨셉
- **아이덴티티: 동그라미 + 사각형 조합** (한글명 "동글사각", 영문명 "CIRCLE SQUARE").
- 대문 아이콘: 차콜(#3a3a3a) 사각형 뒤에 밝은 회색(#b5b5b5) 원, 외곽선 없음, 왼쪽 정렬.
- 파비콘(`docs/favicon.svg`): 흰 원 + 회색(#7f7f7f) 사각형, 테마 무관 동일.
  파비콘 수정 시 `index.html`의 `favicon.svg?v=N` 버전을 올려 캐시 무효화.
- 배경: 중간 톤 회색(#cccccc).
- 이미지 모서리: **2시 방향(오른쪽 위)만 각지고 나머지 세 모서리는 14px 라운드** — 아이콘 컨셉과 통일.
  이 규칙은 확대 보기 이미지와 토글의 "큰 보기" 아이콘(JS의 `BIG_PATH`)에도 똑같이 적용한다.
  아이콘은 각진 모서리(18,0)에서 시작해 시계 방향으로 돌며 4시·8시·10시에만 arc를 넣는다.
- **그리드 썸네일은 배경 화이트만 0.9로 눌러준다** — 종이 배경이 페이지 회색(#d9d9d9)
  위에서 튀지 않게. 도자기(오브제)는 원래 밝기 그대로다.
  구현: `<svg id="filters">`의 `#dimWhite` 톤 커브(`feComponentTransfer`)를
  `.grid img`가 `filter: url(#dimWhite)`로 참조. 표는 `0 0.2 0.4 0.6 0.8 0.9` —
  입력 0.8 아래는 그대로 통과하고 0.8~1.0 구간만 0.8~0.9로 압축된다.
  실측: 배경 255 → 229, 오브제 176 → 176, 148 → 148.
  - **`color-interpolation-filters="sRGB"`를 지우지 말 것.** 기본값 linearRGB로 두면
    중간톤까지 어두워져서 예전 `brightness(.9)`와 다를 게 없어진다.
  - 예전에는 `brightness(.9)`였는데 그건 전 구간을 곱해서 도자기까지 눌렀다
    (176 → 158). 밝기만 조절하려다 오브제가 같이 죽는다.
  - 확대 보기(`#track img`)에는 적용하지 않는다 — 작품은 원본 그대로 본다.
  - CSS 필터라 `png/` 원본도 `docs/images/`의 파일도 바뀌지 않는다.
- **모아보기 토글**(헤더 우측): 인스타그램식 정사각 그리드 — 전체 톤의 통일성을
  한눈에 확인하는 용도. 라운드 없는 정사각 크롭.
- **모아보기 실루엣 겹층**: 열이 늘수록(칸이 작아질수록) 썸네일 위에 실루엣
  (`images/si/NN.svg`)이 진하게 겹쳐져 형태만 남는다. 진하기는 CSS 변수 `--sil`
  (3열=0 → 10열=1) 하나로 조절 — `.sil` 층의 opacity. 브라우저 계산 없음(4절 참조).
- **토글은 열 수까지 한 버튼으로 순환한다**: 큰 보기 → 3열 → 4 → … → 10열 → 큰 보기.
  작품이 늘어도 한 화면에 담기게 하려는 것 — 새 UI를 붙이는 대신 있던 버튼을 늘렸다.
  상태는 열 수 하나(`cols`)로 표현한다: 0이 큰 보기, 3~10이 모아보기. 열 수는 화면에
  표시하지 않는다(그리드 자체로 보인다). `aria-label`에만 남겨 스크린리더가 읽게 한다.
  열 수 자체는 CSS 변수 `--cols`가 바꾼다.
- **토글 아이콘은 "지금 상태"가 아니라 "다음에 누르면 갈 곳"을 그린다.** 2단계 토글일
  땐 아이콘 두 개를 CSS로 갈아끼웠지만, 순환이 여러 단계가 되면서 그 방식은 거짓말이
  된다(3열인데 "큰 보기" 사각형이 떠 있고 실제로 누르면 4열). `setCols()`가
  `drawToggleIcon(next)`로 매번 새로 그린다.
  - 다음이 큰 보기면 둥근 사각형, 아니면 `next×next` 점 격자.
  - 점:간격은 **10:3** — n=3일 때 예전 아이콘(5px 점, 1.5px 간격)과 정확히 같아진다.
  - 격자는 **5에서 멈춘다.** 그 위로 가면 18px에서 점이 뭉개져 큰 보기 사각형과
    구분이 안 된다. 6~9열 구간은 전부 5×5로 보인다(밀도는 그리드 자체로 보이므로 무방).
- **기본 뷰는 화면 폭으로 갈린다**: 600px 이하(폰)는 모아보기 3열, 그 위는 큰 보기.
  폰에서 큰 보기는 1열이라 스크롤만 길어지고 전체 톤이 안 보인다. 최초 1회만
  적용하고 그 뒤로는 사용자가 누른 선택을 끝까지 존중한다(창 크기를 바꿔도 안 뒤집힘).
- **모아보기 = 항상 랜덤 순서.** 모아보기에 **닿을 때마다** 새로 섞는다:
  (1) 새로고침·첫 진입이 모아보기일 때, (2) 로고 클릭,
  (3) 토글로 모아보기에 들어갈 때, (4) **모아보기 안에서 열 수만 바꿀 때도.**
  즉 토글을 누를 때 결과가 모아보기이기만 하면 무조건 섞인다 — 같은 그림들을
  매번 새 조합으로 보는 게 목적이므로 "밀도만 바꾸는 중"이라는 예외를 두지 않는다.
  덕분에 토글 핸들러에 sq/th 변형 경계를 따로 챙기는 분기도 필요 없다(항상 다시 그림).
  같은 그림들을 매번 새 조합으로 보는 게 목적 — 모아보기는 개별 작품이 아니라
  전체 톤을 읽는 화면이라 시간순이 의미가 없다.
- **큰 보기 = 항상 최신순**(번호 역순, `images.json` 순서). 로고를 눌러도 안 섞이고,
  모아보기에서 토글로 돌아오면 최신순이 복원된다. 그래서 원본 순서를 `original`
  배열에 따로 보관한다.
- 섞을 때는 그리드 DOM이 아니라 `names` 배열을 섞고 다시 그린다 — 확대 보기가 같은
  배열을 보므로, DOM만 뒤집으면 스와이프 순서가 화면과 어긋난다.
- 로고 클릭은 위와 별개로 항상 원↔사각형 앞뒤를 뒤집는다.
- 푸터: "동글사각 | CIRCLE SQUARE © 2026", 우측 정렬, 얇은 산세리프, 연회색(#e2e2e2).

## 7. 사이트 구성 (참고)
- 순수 정적 HTML 1장 (`docs/index.html`), 빌드 도구·의존성 없음.
- GitHub Pages 설정: `main` 브랜치 `/docs` 폴더 (설정 완료됨).
- 저장소: https://github.com/suokkim/ceramic

## 8. 확대 뷰어 기술 노트 (iOS 삽질 기록, 2026-08)

뷰어를 스크롤 스냅으로 만들었다가 iOS에서 연쇄 장애(순환 멈춤, 엉뚱한 이미지, 먹통)를
겪고 전면 재작성했다. 같은 실수를 반복하지 않기 위한 기록.

### 최종 구조
- **3장(이전·현재·다음)만 유지하는 transform 페이저.** 스크롤을 아예 쓰지 않는다.
- 순환은 mod 연산 — "끝"이라는 개념이 없어 경계 버그가 원천 차단된다.
- 전환 확정은 `transitionend`가 아니라 **타이머**(유실돼도 반드시 돈다), 그리고 모든
  입력(터치 시작·넘김·열기)이 진행 중 전환을 즉시 확정하고 시작한다 — 상태가 꼬여도
  다음 터치가 리셋하므로 먹통이 구조적으로 불가능(self-healing).
- 제스처는 **터치 이벤트 + 별도 마우스 이벤트**. 포인터 캡처는 iOS에서 불안정해 금지.
- 확대 화면은 `<dialog>`가 아니라 평범한 `position:fixed` div.

### iOS에서 하면 안 되는 것 (전부 실제로 겪음)
- **`scroll-snap` 기반 캐러셀**: iOS는 프로그램 스크롤(scrollLeft, scrollIntoView)을
  스냅으로 되돌리거나 무시한다. 경계 순간이동·재정렬이 간헐적으로 실패해 갇힌다.
- **배경 잠금으로 `body { position:fixed; top:-스크롤량 }`**: body가 큰 음수 top으로
  고정되면 iOS가 내부 fixed 요소를 화면에는 제대로 그리면서 **터치 판정은 어긋난
  위치로** 한다. 이미지는 보이는데 아무 터치도 안 먹는다(페이지를 많이 내린 상태일수록
  심함). 잠금은 오버레이 전 화면 덮기 + 트랙 `touch-action:none`으로 충분하다.
- **문서 레벨 터치 리스너 생략**: `document.addEventListener('touchstart', ()=>{},
  {passive:true})`가 없으면 iOS가 터치를 제대로 전달하지 않는다. `index.html`에 주석과
  함께 있음 — **지우면 뷰어가 실기기에서 죽는다.**
- 이미지 애니메이션 키프레임에 `opacity:0` + `backwards`: 애니메이션이 멈추면 투명하게
  갇혀 이미지가 사라진다. 다수 요소에 transform 애니메이션 동시 적용도 금지(GPU 레이어
  폭증). 애니메이션은 보이는 한 장에만.

### 탭 햅틱(진동)
- 로고·모아보기 토글·확대 열기/닫기·스와이프 넘김에 짧은 진동을 준다.
- 안드로이드/크롬: `navigator.vibrate(10)`.
- **아이폰: Vibration API 자체가 없다.** 대신 숨겨둔 `<input type="checkbox" switch>`(`#hap`)를
  탭 대상 `<label>`이 토글하게 해 시스템 햅틱을 빌린다(iOS 17.4+). 그래서 로고·토글·
  썸네일·확대 화면이 전부 `<label for="hap">`이다(썸네일은 `display:contents` 래퍼라
  그리드 배치는 그대로) — 되돌리면 아이폰 진동이 사라진다.
  `#hap`은 `position:fixed` — label 활성화가 스위치에 포커스를 주므로 화면 밖에 있으면
  브라우저가 거기로 스크롤해 페이지가 튄다.
- 이 편법은 **사용자가 직접 탭할 때만** 동작한다. 스와이프·프로그램 호출에는 안 걸린다.

### 폰에서 테스트하는 법
- GitHub Pages는 HTML을 **10분 캐시**한다. 폰 확인 전 주소 뒤에 `?아무거나`를 붙여
  캐시를 우회할 것. 버전이 헷갈리면 푸터에 임시 `· bN` 표식을 넣었다 뺀다.
- 데스크톱에서 재현 안 되는 iOS 문제는 추측 반복 대신 **`?debug` 화면 로거**(이벤트·
  에러·상태를 페이지 하단에 표시하는 조건부 코드)를 임시로 넣어 폰 화면에서 직접
  읽는 게 가장 빠르다. 이 방법으로 터치 리스너 문제를 한 번에 특정했다.
