# 동글사각 | CIRCLE SQUARE — 전체 작업 프로세스

그림 그리는 것 말고는 전부 자동이다. 이 문서는 아침 캔버스 생성부터 사이트 배포까지
무엇이 언제 어떻게 돌아가는지의 흐름을 정리한 것. 세부 정책·규격은 [POLICY.md](POLICY.md).

```
 08:00 자동                Krita 작업              단축키 ⌘⇧1                 자동 (감시)
┌───────────┐   ┌──────────────────┐   ┌────────────────────┐   ┌─────────────────────────┐
│ newdoc.sh  │ → │ 그림: 작품 레이어 │ → │ krita-export-sil.py │ → │ autopush.sh             │
│ template   │   │ 실루엣: sil 레이어│   │ png/NN.png          │   │  → optimize.sh          │
│ .kra 복사  │   │ (선택)            │   │ png/NN_sil.png (선택)│   │  → git 커밋·푸시        │
└───────────┘   └──────────────────┘   └────────────────────┘   │  → 1~2분 뒤 사이트 반영 │
                                                                 └─────────────────────────┘
```

## 1. 아침 — 캔버스 자동 생성 (08:00)

- launchd(`com.ceramic.newdoc`)가 매일 08:00 `newdoc.sh` 실행.
- **`tools/template.kra`** 를 `kra/MMDD_NN.kra`로 복사해 Krita로 연다.
  - A4 300dpi 2480×3508. 구성: 작품 그룹(이름=번호, 안에 sil·sign·그림) + 흰 배경 — 그룹 이름은 생성 때 자동 기입.
  - NN = png/와 kra/를 둘 다 보고 정한 다음 갤러리 번호 (`tools/numof.sh` 규칙).
- 오늘 캔버스가 이미 있으면 새로 만들지 않고 열기만 한다.
- 같은 날 캔버스 추가: `./newdoc.sh -n`.

## 2. 낮 — Krita 작업

### 작품
그림 레이어에 평소처럼 그린다.

**하루 한 파일에 작품 여러 개**를 그릴 때는 최상위 레이어(그룹) 이름을 번호로:
```
0809_18        ← 작품 (그룹이면 안은 자유)
0809_18_sil    ← 18번 실루엣 (선택)
0809_18_v2     ← 18번의 다른 버전 (선택 — 뷰어에서 좌우 스와이프로 보임)
0809_19        ← 다음 작품
Layer 1        ← 번호 없는 레이어 = 공용 배경 (모든 작품에 깔림)
```

**같은 작품의 여러 버전**: `번호_v2`, `번호_v3` … 이름의 레이어(그룹)로 그린다.
원작(접미사 없음)이 대표판 — 그리드·모아보기·실루엣은 원작만 쓰고, 버전은
뷰어에서 좌우 스와이프로만 나온다(하단에 ● ○ 점 표시).

### 실루엣 (선택)
모아보기에서 열 수를 올리면 작품이 실루엣으로 변하는데, 기본은 **자동 계산**이라
아무것도 안 해도 된다. 자동 결과가 마음에 안 드는 작품만 직접 그린다:
- 단일 작품 문서 → 템플릿에 있는 `sil` 레이어에.
- 다작품 문서 → `번호_sil` 이름의 레이어에 (위 예시).
- 빈 실루엣 레이어는 무시된다 → 그 작품은 자동 계산 유지.

## 3. 내보내기 — 단축키 한 번 (⌘⇧1)

`tools/krita-export-sil.py` (Ten Scripts 슬롯 1)가 문서를 보고 알아서 처리:

| 문서 상태 | 동작 |
|---|---|
| 번호 레이어 없음 (단일 모드) | 전체(sil 제외) → `png/문서명.png`, sil 레이어에 그림 있으면 → `png/문서명_sil.png` |
| 번호 레이어 있음 (다작품 모드) | 번호 레이어마다: 그 레이어+공용만 켜고 → `png/번호.png`, `번호_sil` → `png/번호_sil.png` |

- 파일명·폴더 타이핑 없음. 레이어 가시성은 내보낸 뒤 원상복구.
- 수정도 같은 키 — 문서(레이어) 이름이 같으니 같은 파일을 덮어쓰고 그 자리만 갱신된다.

## 4. 배포 — 전부 자동

`png/` 폴더가 바뀌는 순간 감시 에이전트(`com.ceramic.autopush` → `autopush.sh`)가:

0. **png/ 지문 대조** — 파일명·크기·수정시각이 지난번과 같으면 즉시 종료.
   launchd는 내용이 안 바뀌어도 폴더 메타데이터 이벤트만으로 발동해서, 이게 없으면
   앱이 혼자 뜨고 지기를 반복한다(실제로 겪었다). 동시 실행 방지 락도 함께 건다.
1. 파일 크기가 멈출 때까지 대기 (내보내는 중인 파일 보호)
2. **`tools/optimize.sh`** 실행:
   - `docs/images/NN.png` — 웹용 원본 (긴 변 1600px)
   - `docs/images/th/NN.jpg` — 썸네일 600px (큰 보기용)
   - `docs/images/sq/NN.jpg` — 정사각 240px (모아보기 작은 칸용)
   - `docs/images/si/NN.svg` — **실루엣 벡터** (`tools/silhouette.mjs`):
     `png/NN_sil.png`이 있으면 손그림 그대로 벡터화(`--manual`),
     없으면 1600px 원본에서 자동 계산(경계 추출 → 노이즈·그림자 정리 → RDP 단순화)
   - `docs/images/NN-2.png` — 버전(`png/MMDD_NN_v2.png`). 뷰어 전용이라 1600px
     PNG만 만들고 썸네일·실루엣은 원작 것을 공유한다.
   - `docs/images.json` 목록·`og:image` 갱신. 원본 해시가 바뀐 것만 다시 만든다.
     목록은 작품당 배열 `[원작, v2, …]` — 첫 요소가 대표판.
3. 바뀐 게 있으면 git 커밋 → 푸시
4. **1~2분 뒤 https://suokkim.github.io/ceramic/ 반영** (HTML은 10분 캐시 — 확인은 주소 뒤 `?1`)

## 5. 사이트에서 보이는 것

- **큰 보기**: 최신순, 탭하면 확대 뷰어. 위아래 스와이프 = 작품 넘김(순환),
  좌우 스와이프 = 같은 작품의 버전 넘김, 핀치 = 확대.
- **모아보기**(우상단 토글): 탭마다 3열 → … → 8열 → 큰 보기 순환. 항상 최신순.
- **실루엣 모드**(로고 토글): 사각형이 앞 = 실루엣(si/NN.svg), 원이 앞 = 입체.
  진하기는 CSS 변수 `--sil` 하나, 브라우저 계산 없음.

## 6. 손이 가는 경우만

| 상황 | 방법 |
|---|---|
| 작품 수정 | 그 문서 열어 고치고 ⌘⇧1 — 끝 |
| 수제 실루엣 취소 | `png/NN_sil.png`·`docs/images/si/NN.svg` 삭제 → `./tools/optimize.sh` |
| 갤러리 순서 변경 | `png/` 파일 번호 수정 → `./tools/optimize.sh` |
| 수동 배포 | `./tools/optimize.sh` 후 직접 커밋·푸시 (autopush와 결과 동일) |
| 자동 실행 로그 | `/tmp/ceramic-newdoc.log`, `/tmp/ceramic-autopush.log` |
| 자동 켜고 끄기 | `launchctl bootout/bootstrap gui/$UID ~/Library/LaunchAgents/com.ceramic.*.plist` |

## 7. 구성 요소

| 파일 | 역할 |
|---|---|
| `tools/template.kra` | 새 캔버스 원형 (배경 + 그림 + 숨긴 sil 레이어) |
| `newdoc.sh` | 아침 캔버스 생성 (번호 발급, 중복 방지) |
| `tools/numof.sh` | 파일명 → 번호 규칙의 단일 소스 (`sh tools/numof.sh --test`) |
| `tools/krita-export-sil.py` | Krita 단축키 내보내기 (작품/실루엣 분리, 단일·다작품 모드) |
| `autopush.sh` + launchd | png/ 감시 → optimize → 커밋·푸시 |
| `tools/optimize.sh` | 웹용 4종 생성 + 목록 갱신 (바뀐 것만) |
| `tools/silhouette.mjs` | 실루엣 SVG 생성 (자동 계산 / `--manual` 손그림 벡터화) |
| `docs/` | GitHub Pages 사이트 루트 (정적 HTML 1장, 의존성 없음) |
